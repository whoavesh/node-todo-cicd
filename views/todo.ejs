<!DOCTYPE html>

<html>

<head>
    <title>Development Automation</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Ubuntu+Mono&display=swap');

        a,
        li {
            list-style: none;
            text-decoration: none;
            color: white;
        }

        body {
            text-decoration: none;
            background-color: #03001C;
            color: #6c5ce7;
            font-family: 'Ubuntu Mono', monospace;
            font-size: 1.5vw;
        }

        h1,
        ul {
            text-align: center;
        }

        label{ font-size:2vw;}
        form {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .inputelem {
            display: flex;
            flex-flow: row wrap;
            align-items: center;
            outline: none;
            margin: 9px;
        }

        .inputelem input {
            margin-right: 9px;
        }

        .inputelem input[type=text]:focus {

            outline: none;
        }



        button {
            border: none;
            outline: none;
            background-color: #6c5ce7;
            padding: 10px 20px;
            font-size: 12px;
            font-weight: 700;
            color: #fff;
            border-radius: 5px;
            transition: all ease 0.1s;
            box-shadow: 0px 5px 0px 0px #a29bfe;
        }

        button:active {
            transform: translateY(5px);
            box-shadow: 0px 0px 0px 0px #a29bfe;
        }


        /* input */
        input {
            width: 210px;
            height: 40px;
            padding: 0 16px;
            background: transparent;
            border-radius: 4px;
            border: 1px solid #a29bfe;
            color: #f9f9f9;
        }
    </style>
</head>

<body>

    <div class="animate__animated animate__fadeInUp">
        <h1>This application is deployed using automation</h1>
        <ul>
            <% todolist.forEach(function(todo, index) { %>
                <li>
                    <a href="/todo/delete/<%= index %>">✘</a>
                    <a href="/todo/<%= index %>">✎</a>
                    <%- todo %>
                </li>
                <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <p>
                <label for="newtodo">What shoud I do?</label>
            <div class="inputelem">
                <input type="text" placeholder="Type here" name="newtodo" id="newtodo" autofocus />
                <button type="submit" value="Add">
                    Add
                </button>
            </div>
            </p>
        </form>

    </div>
</body>

</html>
